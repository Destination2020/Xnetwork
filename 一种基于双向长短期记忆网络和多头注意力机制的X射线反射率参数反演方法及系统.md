# 权利要求书

1、一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演方法，其特征在于，包括以下步骤：

S1、获取待测薄膜样品的X射线反射率曲线，并对所述X射线反射率曲线进行预处理，得到用于神经网络输入的反射率序列；

S2、获取或构建待测薄膜样品中各目标材料层及衬底材料的散射长度密度先验信息，将所述散射长度密度先验信息形成辅助输入特征；

S3、将所述反射率序列输入双向长短期记忆网络，以提取反射率曲线中沿入射角方向分布的双向序列特征；

S4、将所述双向序列特征输入多头注意力模块，对不同入射角区间、临界角区域、振荡区域及高角度衰减区域的特征贡献进行自适应加权，得到全局反射率表征；

S5、将所述全局反射率表征与所述辅助输入特征融合，并通过参数回归模块输出薄膜结构参数、界面参数、材料光学参数和仪器修正参数；

S6、基于输出的参数构建薄膜层状结构模型，并采用X射线反射率物理正演模型重建反射率曲线，将重建曲线与实测曲线进行比较，以完成参数反演结果的物理一致性校验。

2、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，步骤S1中，所述预处理包括对反射率曲线进行角度范围统一、采样点数统一、异常值剔除、非正值截断、对数变换及归一化处理；

所述采样点数统一包括将不同实验数据或仿真数据插值、裁剪或补齐为预设长度的反射率序列。

3、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，步骤S2中，所述散射长度密度先验信息包括目标材料层及衬底材料的散射长度密度实部估计值和散射长度密度虚部估计值；

所述散射长度密度先验信息可以由材料数据库、理论计算、经验估计、单独标定结果或用户输入获得，并通过归一化后输入先验编码网络。

4、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，步骤S3中，所述双向长短期记忆网络沿反射率序列的正向和反向同时提取序列特征，以同时捕获低角度区域的临界角信息、高角度区域的衰减信息以及振荡周期对应的层厚信息；

所述双向长短期记忆网络包括不少于一层的双向长短期记忆单元，并输出包含正向隐藏状态和反向隐藏状态的序列特征。

5、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，步骤S4中，所述多头注意力模块包括多个并行注意力头，每个注意力头分别生成查询矩阵、键矩阵和值矩阵，并通过缩放点积注意力计算不同反射率序列位置之间的相关性；

所述多头注意力模块还包括残差连接、层归一化及前馈网络，以提高对弱振荡、局部峰谷偏移和噪声扰动的鲁棒性。

6、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，步骤S5中，所述参数回归模块输出的薄膜结构参数包括至少一个薄膜层的厚度；

所述界面参数包括空气或表面层与薄膜层之间的界面粗糙度、薄膜层之间的界面粗糙度以及薄膜层与衬底之间的界面粗糙度；

所述材料光学参数包括薄膜层及衬底的散射长度密度实部和散射长度密度虚部；

所述仪器修正参数包括角度偏移参数和分辨率展宽参数。

7、根据权利要求6所述的X射线反射率参数反演方法，其特征在于，所述参数回归模块还输出表面污染层、氧化层、吸附层或过渡层参数，所述表面污染层、氧化层、吸附层或过渡层参数包括表面层厚度、表面层散射长度密度实部和表面层散射长度密度虚部；

在输出所述表面层参数时，对表面层厚度、表面层粗糙度及表面层散射长度密度虚部施加物理约束，以避免出现不符合层状薄膜结构物理意义的参数组合。

8、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，步骤S6中，所述X射线反射率物理正演模型采用传递矩阵法计算层状结构的反射率曲线；

所述传递矩阵法根据各层厚度、界面粗糙度和复折射率计算反射振幅，并得到反射率强度，其中复折射率由散射长度密度实部、散射长度密度虚部和X射线波长确定。

9、根据权利要求1所述的X射线反射率参数反演方法，其特征在于，所述神经网络训练过程包括：基于层状结构参数随机采样生成训练样本，采用传递矩阵法生成对应X射线反射率曲线，并向所述反射率曲线中加入角度偏移、强度缩放、仪器展宽、背景噪声和随机噪声，以构建接近实测条件的训练数据集；

训练时采用平均绝对误差或均方误差作为损失函数，并采用自适应优化器对神经网络参数进行更新。

10、一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演系统，用于执行权利要求1至9任一项所述的X射线反射率参数反演方法，其特征在于，包括：

数据采集与输入模块，用于获取实测或仿真的X射线反射率曲线；

曲线预处理模块，用于对反射率曲线进行角度范围统一、采样点数统一、对数变换及归一化处理；

先验特征输入模块，用于输入并归一化目标材料层和衬底材料的散射长度密度先验信息；

序列特征提取模块，包括双向长短期记忆网络，用于提取反射率曲线的双向序列特征；

注意力加权模块，包括多头注意力机制，用于对不同角度区间和不同曲线特征进行自适应加权；

参数回归模块，用于输出薄膜厚度、界面粗糙度、散射长度密度、表面层参数及仪器修正参数；

物理一致性校验模块，用于根据输出参数重建X射线反射率曲线，并将重建曲线与实测曲线进行比较。

# 说明书

## 一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演方法及系统

## 技术领域

本发明涉及X射线薄膜表征、计算物理反演和人工智能材料分析领域，尤其涉及一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演方法及系统。

## 背景技术

X射线反射率技术是一种常用于薄膜、界面和多层结构表征的无损检测方法。通过测量样品在不同掠入射角下的反射强度，可以获得薄膜厚度、界面粗糙度、电子密度或散射长度密度等结构与材料信息。X射线反射率曲线通常包含临界角、Kiessig振荡、振荡包络衰减以及高角度尾部等特征，这些特征与薄膜的厚度、密度和界面质量密切相关。

传统的X射线反射率参数反演通常依赖遗传算法、粒子群优化、模拟退火、最小二乘拟合或人工调参等方法。上述方法需要反复调用物理正演模型，并在多维参数空间中进行搜索。当待测结构包含多个薄膜层、过渡层、表面污染层或未知材料光学常数时，参数之间存在较强耦合，容易出现多解性、局部最优和拟合速度慢的问题。

此外，实际实验曲线还会受到仪器分辨率展宽、角度零点偏移、背景噪声、强度归一化误差以及材料光学常数不确定性的影响。传统拟合方法通常需要人工设置初始值和参数边界，对操作者经验依赖较强，难以实现高通量、自动化和实时化的薄膜结构分析。

近年来，深度学习方法被用于曲线反演和材料参数预测。然而，普通全连接网络或卷积网络在处理X射线反射率曲线时，往往难以同时兼顾曲线的长程序列依赖和局部关键特征。X射线反射率曲线沿入射角方向具有明显的序列属性：低角度区域反映临界角和密度信息，中角度振荡周期反映膜厚信息，高角度衰减和峰谷对比度反映粗糙度及吸收信息。若不充分建模这些区域之间的关系，网络容易只学习局部幅值模式，而忽略全局物理约束。

因此，亟需一种能够结合X射线反射率曲线序列特征、材料散射长度密度先验信息和物理正演校验机制的参数反演方法，以提高薄膜参数反演的速度、稳定性和抗噪能力。

## 发明内容

针对现有X射线反射率参数反演中存在的人工依赖强、搜索速度慢、参数耦合严重、实测噪声适应能力不足以及神经网络物理解释性不足等问题，本发明提供了一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演方法及系统。

本发明通过将X射线反射率曲线视为沿入射角排列的一维物理序列，利用双向长短期记忆网络提取曲线正向和反向的长程依赖关系，并利用多头注意力机制对不同角度区域的物理特征进行自适应加权。进一步地，本发明引入散射长度密度先验信息作为辅助输入，以增强网络对材料光学参数不确定性的适应能力。神经网络输出薄膜结构参数后，再通过传递矩阵法重建反射率曲线，对反演结果进行物理一致性校验。

一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演方法，包括以下步骤：

S1、获取待测薄膜样品的X射线反射率曲线，并对所述X射线反射率曲线进行预处理，得到用于神经网络输入的反射率序列；

S2、获取或构建待测薄膜样品中各目标材料层及衬底材料的散射长度密度先验信息，将所述散射长度密度先验信息形成辅助输入特征；

S3、将所述反射率序列输入双向长短期记忆网络，以提取反射率曲线中沿入射角方向分布的双向序列特征；

S4、将所述双向序列特征输入多头注意力模块，对不同入射角区间、临界角区域、振荡区域及高角度衰减区域的特征贡献进行自适应加权，得到全局反射率表征；

S5、将所述全局反射率表征与所述辅助输入特征融合，并通过参数回归模块输出薄膜结构参数、界面参数、材料光学参数和仪器修正参数；

S6、基于输出的参数构建薄膜层状结构模型，并采用X射线反射率物理正演模型重建反射率曲线，将重建曲线与实测曲线进行比较，以完成参数反演结果的物理一致性校验。

优选地，所述反射率序列的长度为621个采样点，角度范围为0.4000度至3.5000度，所述采样点数和角度范围也可以根据仪器条件、样品结构和测量需求进行调整。

优选地，所述辅助输入特征包括三类材料对象的散射长度密度实部和虚部，所述三类材料对象可以为第一薄膜层、第二薄膜层和衬底，也可以扩展为更多薄膜层、缓冲层或过渡层。

优选地，所述参数回归模块输出17个参数，包括两个主薄膜层厚度、四个界面粗糙度、三个散射长度密度实部、三个散射长度密度虚部、三个表面层参数、一个仪器展宽参数和一个角度偏移参数。

优选地，所述表面层参数用于描述空气与主薄膜层之间的污染层、吸附层、氧化层、尘埃层或渐变过渡层，以减小实测曲线中表面非理想因素对主薄膜参数反演的影响。

优选地，所述物理一致性校验包括：根据神经网络输出参数计算复折射率；利用传递矩阵法计算理论X射线反射率曲线；对理论曲线施加仪器展宽、角度偏移、背景补偿和强度缩放；将处理后的理论曲线与实测曲线在对数域中进行误差比较。

与现有技术相比，本发明至少具有以下有益效果：

1、反演速度快：训练完成后，神经网络可以直接由X射线反射率曲线预测薄膜参数，避免传统搜索算法大量重复调用正演模型，显著提高参数反演效率。

2、曲线特征利用充分：双向长短期记忆网络能够同时利用低角度至高角度方向和高角度至低角度方向的序列信息，多头注意力机制能够自适应关注临界角、振荡峰谷、衰减包络等关键区域。

3、参数耦合适应能力强：通过引入散射长度密度先验信息，网络能够在材料光学常数存在不确定性的情况下保持较稳定的参数反演能力。

4、抗噪性能好：训练样本中引入角度偏移、分辨率展宽、背景噪声和随机扰动，使模型能够适应实际实验曲线中的仪器误差和噪声干扰。

5、具备物理一致性：通过传递矩阵法对神经网络输出参数进行反射率曲线重建，使反演结果不仅来自数据驱动预测，也受到X射线反射物理模型的约束和验证。

## 附图说明

图1为本发明方法的整体流程示意图；

图2为本发明中神经网络结构示意图，包括反射率序列输入、双向长短期记忆网络、多头注意力模块、散射长度密度先验编码模块及参数回归模块；

图3为本发明中训练数据生成流程示意图，包括随机层状结构采样、传递矩阵法正演、仪器误差与噪声加入以及标签构建；

图4为本发明中物理一致性校验流程示意图，包括预测参数、重建反射率曲线、曲线补偿和误差评估；

图5为本发明系统组成示意图，包括数据采集与输入模块、曲线预处理模块、先验特征输入模块、序列特征提取模块、注意力加权模块、参数回归模块和物理一致性校验模块。

## 具体实施方式

下面结合实施例对本发明的技术方案进行详细说明。应当理解，以下实施例仅用于说明本发明，并不用于限制本发明的保护范围。

本实施例以双层薄膜样品的X射线反射率参数反演为例进行说明。所述双层薄膜样品包括空气侧表面层、第一薄膜层、第二薄膜层和衬底层。其中，第一薄膜层可以为TiO2层，第二薄膜层可以为铌酸盐复合层，衬底层可以为Si衬底。本发明的方法也适用于单层膜、多层膜、超晶格、周期膜、非周期多层膜和具有表面污染层的薄膜结构。

S1、反射率曲线获取与预处理。

通过X射线反射率仪采集待测样品在预设入射角范围内的反射率曲线。对于来自不同仪器或不同测量设置的数据，首先将其统一为相同的角度范围和采样点数。例如，将0.4000度至3.5000度范围内的曲线插值为621个采样点。随后，对反射率中的非数值、无穷值和非正值进行处理，将小于预设下限的反射率截断为正数下限，并进行对数变换，以压缩反射率跨越多个数量级带来的动态范围。

设原始反射率序列为：

$$
R = \{R_1, R_2, \ldots, R_N\}
$$

其中，$N$为采样点数。预处理后的反射率序列可以表示为：

$$
X_i = \log_{10}(\max(R_i,\epsilon) + \epsilon)
$$

其中，$\epsilon$为避免数值不稳定的正数常量。

S2、散射长度密度先验输入。

由于X射线反射率曲线同时受厚度、粗糙度和材料光学常数影响，仅依靠反射率曲线进行完全盲反演容易出现多解性。本实施例引入散射长度密度先验信息作为辅助输入。所述散射长度密度先验信息包括第一薄膜层、第二薄膜层和衬底的散射长度密度实部估计值以及散射长度密度虚部估计值。

设散射长度密度先验输入为：

$$
F = [\rho'_{1}, \rho'_{2}, \rho'_{s}, \rho''_{1}, \rho''_{2}, \rho''_{s}]
$$

其中，$\rho'$表示散射长度密度实部，$\rho''$表示散射长度密度虚部，下标1、2和s分别表示第一薄膜层、第二薄膜层和衬底。对所述先验输入进行范围归一化后，将其输入先验编码网络。该先验编码网络可以由多层全连接网络和非线性激活函数构成。

S3、双向长短期记忆网络序列特征提取。

将预处理后的反射率序列输入双向长短期记忆网络。双向长短期记忆网络包括正向长短期记忆单元和反向长短期记忆单元，正向长短期记忆单元沿入射角增大的方向提取曲线特征，反向长短期记忆单元沿入射角减小的方向提取曲线特征。

X射线反射率曲线中，临界角附近的信息通常与材料密度和衬底性质有关，振荡周期与膜厚有关，峰谷对比度和高角度衰减与粗糙度及吸收有关。通过双向长短期记忆网络，可以在同一特征空间中融合低角度、中角度和高角度之间的长程依赖关系，避免仅依赖局部窗口造成的参数误判。

S4、多头注意力加权。

将双向长短期记忆网络输出的序列隐藏状态输入多头注意力模块。所述多头注意力模块对反射率序列的不同位置进行相关性计算，得到不同角度区域对最终参数反演的贡献权重。不同注意力头可以分别关注临界角区域、低阶振荡峰谷、高阶振荡细节和高角度噪声衰减区域。

对于第$h$个注意力头，其缩放点积注意力可以表示为：

$$
Attention(Q_h,K_h,V_h)=softmax\left(\frac{Q_hK_h^T}{\sqrt{d_k}}\right)V_h
$$

其中，$Q_h$、$K_h$和$V_h$分别为查询矩阵、键矩阵和值矩阵，$d_k$为键向量维度。多个注意力头的输出经拼接和线性映射后，得到全局反射率表征。

优选地，多头注意力模块包括残差连接和层归一化。残差连接能够保持原始序列表征，层归一化能够增强训练稳定性，前馈网络能够进一步进行非线性特征变换。

S5、特征融合与参数回归。

先验编码网络输出的辅助特征与多头注意力模块输出的全局反射率表征进行拼接，形成融合特征。所述融合特征输入参数回归模块，参数回归模块输出薄膜结构参数和仪器修正参数。

在一个优选实施例中，输出参数包括：

第一薄膜层厚度、第二薄膜层厚度；

空气或表面层与第一薄膜层之间的界面粗糙度、表面层与第一薄膜层之间的界面粗糙度、第一薄膜层与第二薄膜层之间的界面粗糙度、第二薄膜层与衬底之间的界面粗糙度；

第一薄膜层、第二薄膜层和衬底的散射长度密度实部；

第一薄膜层、第二薄膜层和衬底的散射长度密度虚部；

表面层厚度、表面层散射长度密度实部、表面层散射长度密度虚部；

仪器分辨率展宽参数和角度偏移参数。

为避免神经网络输出超出物理范围，本实施例可以将网络输出限制在归一化区间，并通过反归一化映射到真实物理参数范围。同时，可以对表面层粗糙度不大于表面层厚度、散射长度密度虚部不超过散射长度密度实部的预设比例等条件施加约束。

S6、物理一致性校验与曲线重建。

根据神经网络输出的薄膜参数构建层状结构模型。对于每一层，根据散射长度密度实部和虚部计算复折射率：

$$
n_j = 1 - \frac{\lambda^2}{2\pi}\rho'_j + i\frac{\lambda^2}{2\pi}\rho''_j
$$

其中，$n_j$表示第$j$层复折射率，$\lambda$表示X射线波长，$\rho'_j$和$\rho''_j$分别表示第$j$层散射长度密度实部和虚部。

随后采用传递矩阵法计算理论反射率曲线。对于相邻层之间的界面，考虑界面粗糙度的Nevot-Croce修正项，以增强对真实薄膜界面的描述能力。理论曲线计算完成后，对其施加高斯展宽、角度偏移、背景补偿和强度缩放，使其与实测仪器条件相匹配。

物理一致性校验可在对数域中计算重建曲线与实测曲线之间的误差：

$$
E = \frac{1}{N}\sum_{i=1}^{N}\left(\log_{10}(R^{pred}_i+\epsilon)-\log_{10}(R^{exp}_i+\epsilon)\right)^2
$$

若误差低于预设阈值，则输出参数反演结果；若误差高于预设阈值，则可对背景、角度偏移和强度缩放进行局部补偿搜索，或者将神经网络输出作为传统拟合算法的初始值进一步优化。

S7、训练数据构建。

为了使神经网络适应不同薄膜结构和实验误差，本实施例采用物理仿真方式构建训练数据。首先在预设参数范围内随机采样薄膜厚度、界面粗糙度、散射长度密度、表面层参数、仪器展宽和角度偏移。然后基于传递矩阵法生成对应X射线反射率曲线。进一步地，对曲线加入强度缩放、背景噪声、平滑噪声、角度偏移和分辨率展宽，以模拟真实实验条件。

训练样本由反射率序列、散射长度密度先验输入和参数标签构成。训练时，网络预测参数经归一化后与标签进行比较，损失函数可以采用平均绝对误差：

$$
L = \frac{1}{M}\sum_{m=1}^{M}|y_m-\hat{y}_m|
$$

其中，$y_m$为真实标签，$\hat{y}_m$为网络预测值，$M$为参数数量。优化器可以采用AdamW优化器，学习率调度可以采用余弦退火策略，以提高训练收敛稳定性。

本发明还提供一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演系统。所述系统包括数据采集与输入模块、曲线预处理模块、先验特征输入模块、序列特征提取模块、注意力加权模块、参数回归模块和物理一致性校验模块。

数据采集与输入模块用于获取来自X射线反射率仪的实验曲线，也可以读取由传递矩阵法生成的仿真曲线。曲线预处理模块用于完成角度统一、采样统一、对数变换和异常值处理。先验特征输入模块用于接收材料散射长度密度先验并输出编码特征。序列特征提取模块用于通过双向长短期记忆网络提取反射率曲线特征。注意力加权模块用于突出关键角度区间和关键振荡特征。参数回归模块用于输出待测样品参数。物理一致性校验模块用于基于预测参数重建曲线并进行误差评估。

本发明通过数据驱动模型与X射线反射物理模型的结合，将传统拟合中耗时的多参数搜索转化为快速神经网络预测，并保留正演模型对结果的校验作用，适合用于薄膜工艺监控、材料数据库构建、在线质量检测和高通量实验分析。

# 说明书摘要

本发明公开了一种基于双向长短期记忆网络和多头注意力机制的X射线反射率参数反演方法及系统。该方法获取待测薄膜样品的X射线反射率曲线，对其进行采样统一、异常值处理、对数变换和归一化处理；同时输入目标材料层及衬底的散射长度密度先验信息；利用双向长短期记忆网络提取反射率曲线沿入射角方向的双向序列特征，并通过多头注意力机制对临界角区域、振荡区域和高角度衰减区域进行自适应加权；将曲线表征与散射长度密度先验特征融合后，输出薄膜厚度、界面粗糙度、散射长度密度、表面层参数、仪器展宽和角度偏移等参数；再采用传递矩阵法重建X射线反射率曲线，对预测参数进行物理一致性校验。本发明能够提高X射线反射率参数反演的速度、稳定性和抗噪能力，减少人工初值依赖，适用于薄膜结构高通量分析和在线质量检测。

# 说明书附图

图1为本发明方法的整体流程示意图。

图2为本发明神经网络结构示意图。

图3为本发明训练数据生成流程示意图。

图4为本发明物理一致性校验流程示意图。

图5为本发明系统组成示意图。

指定图1为摘要附图。
