# 权利要求书

1、一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，包括以下步骤：

S1、利用空间光调制器对经过准直和偏振处理的相干光源进行相位调制，生成一系列确定性散斑图案作为确定性散斑照明场；控制空间光调制器对确定性散斑图案进行平移，并同步采集经待测物镜成像的低分辨率图像序列；

S2、对采集到的低分辨率图像序列进行预处理，初始化样品的复振幅估计值和待测物镜的相干传递函数；根据空间光调制器加载的原始散斑图案，利用傅里叶相移定理计算各平移位置对应的理论照明场复振幅分布；

S3、建立包含样品复振幅、确定性散斑照明场及物镜相干传递函数的联合优化模型，并利用确定性散斑照明场的复振幅分布作为空间域强约束；

S4、通过反向传播误差信号，同步更新样品的复振幅与物镜相干传递函数的相位分布及振幅分布；执行多轮次迭代，直至目标损失函数收敛或达到预设的最大迭代次数；

S5、提取步骤S4恢复得到的相干传递函数的相位部分，拟合成泽尼克多项式以量化分解待测物镜的像差，并结合恢复出的样品振幅和相位信息进行系统性能评估。

2、根据权利要求1所述的基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，步骤S1中，所述的确定性散斑图案是通过计算机预先生成并加载至空间光调制器的灰度图或相位图，其生成算法采用菲涅尔衍射传播、Gerchberg-Saxton算法或伪随机相位矩阵经傅里叶变换；

进行平移的策略包括沿X轴和Y轴的亚像素级步进平移，平移步长设置为物镜截止频率对应的奈奎斯特采样间隔的分数倍。

3、根据权利要求1所述的基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，步骤S2中，所述预处理包括暗场校正、平场校正及坏点像素剔除；

初始化样品的复振幅估计值和待测物镜的相干传递函数具体为：基于所有采集图像的强度平方根平均值初始化样品的复振幅估计值；将待测物镜的相干传递函数初始化为理想圆瞳函数或基于设计参数的先验估计值。

4、根据权利要求1所述的基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，步骤S3中，联合优化模型采用改进的交替投影算法进行迭代更新，具体包括：

在空间域中，将当前估计的样品复振幅与已知平移后的确定性散斑照明场复振幅相乘，计算出射波前；

在傅里叶域中，将出射波前变换至频域，并与当前估计的物镜相干传递函数相乘，模拟光瞳低通滤波效应，随后用实际采集的低分辨率图像强度的平方根替换滤波后波前的振幅，同时保留其相位。

5、根据权利要求1所述的基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，步骤S4中，所述同步更新引入自适应权重更新机制，在更新物镜相干传递函数时，根据频谱信噪比动态调整更新步长：在低频区域采用较大的更新步长，在高频区域采用较小的更新步长，并结合总变分正则化或稀疏约束以防止噪声放大。

6、根据权利要求1所述的基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，步骤S4中，所述目标损失函数定义为重建强度图像与实测强度图像之间的均方误差或泊松似然函数；收敛条件为连续两次迭代间的损失函数变化量小于预设阈值，或者恢复出的波像差RMS值在连续多次迭代中变化小于0.001λ。

7、根据权利要求1所述的基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，步骤S5中，所述量化分解待测物镜的像差具体包括分解出离焦、像散、彗差及球差项；

所述系统性能评估包括对标定后的成像系统进行调制传递函数分析和信噪比评估。

8、一种基于确定性散斑照明的傅里叶叠层物镜像差标定系统，用于执行权利要求1~7任一项所述的傅里叶叠层物镜像差标定方法，其特征在于，包括：

激光准直偏振模块，用于产生高相干性、偏振态稳定的平行光束；

空间光调制模块，与所述激光准直偏振模块光路连接，用于生成并动态调控确定性散斑照明场；所述空间光调制模块包括空间光调制器、偏振控制单元及折叠反射镜；

成像模块，用于接收经样品调制的光束并记录低分辨率强度图像；所述成像模块包括待测物镜、管状透镜及图像传感器；

迭代恢复算法模块，部署于计算单元中，内置有散斑图案生成算法及联合优化重建算法；所述迭代恢复算法模块用于控制空间光调制器和图像传感器同步工作，并利用已知的确定性散斑照明作为先验约束，执行迭代恢复流程，输出样品高分辨率复振幅图像及待测物镜的复值相干传递函数。

9、根据权利要求8所述的基于确定性散斑照明的傅里叶叠层物镜像差标定系统，其特征在于，所述激光准直偏振模块包括单纵模连续波激光光源、由第一透镜和第二透镜构成的扩束准直透镜组，以及起偏器；

若使用宽光谱光源，所述激光准直偏振模块还包括插入光路中的窄带滤光片。

10、根据权利要求8所述的基于确定性散斑照明的傅里叶叠层物镜像差标定系统，其特征在于，所述空间光调制模块中的偏振控制单元包括二分之一波片，用于旋转入射光的偏振方向以匹配空间光调制器的液晶分子长轴方向；

所述空间光调制器为反射式相位型LCOS-SLM，其刷新率不低于60Hz，填充因子大于95%。

# 说明书

## 一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法及系统

## 技术领域

本发明涉及光学系统成像质量检测领域，尤其是涉及一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法及系统。

## 背景技术

傅里叶叠层显微成像（FPM）作为一种融合了相位恢复算法与合成孔径理论的计算成像技术，通过采集一系列不同角度照明下的低分辨率强度图像，能够在傅里叶域内合成超越物镜衍射极限的宽频谱，从而重建出兼具大视场和高分辨率的样品复振幅信息。由于其能够同时获取定量相位信息，FPM在生物医学观测、病理诊断及光学检测领域具有广泛的应用前景。

如公开号为CN111307759A的中国专利文献公开了一种连续太赫兹波傅里叶叠层显微成像系统和方法；公开号为CN118192059A的中国专利文献公开了一种适用于大数值孔径物镜的快速傅里叶叠层显微成像系统。

传统的FPM系统通常采用可编程的LED阵列作为光源，通过依序点亮不同位置的LED单元提供多角度照明，然而这种方案在实际应用中，特别是在高精度的像差标定任务中，面临着LED阵列物理位置偏差大、各单元发光强度不均匀以及光源光谱非单色性等问题，导致频谱拼接错误，且为了获得高质量重建往往需要极高计算成本的校正算法来标定LED参数。

为了克服LED阵列的局限性并提高采样效率，现有技术中出现了基于散射片或磨砂玻璃产生随机散斑场进行照明的FPM方案，虽然这种方案利用了散斑的高频编码特性，但其核心缺陷在于通过散射片产生的散斑场在数学上是随机且完全未知的，这导致在图像重建过程中，算法被迫面对一个高度病态的全盲反演问题，即需要同时求解样品复振幅、未知的照明散斑场以及系统的光瞳函数三个未知量；由于缺乏对照明场的先验约束，这种盲反演算法不仅收敛速度极慢、计算效率低，而且面临着解的模糊性与非唯一性挑战，极易陷入局部最优解，特别是在低信噪比环境下，微弱的波前像差信息极易被照明恢复过程中的误差所淹没，导致无法实现对待测物镜光瞳函数的高精度、高重复性标定。

因此，现有的技术手段难以同时满足系统结构简单、收敛速度快和像差标定精度高的要求，迫切需要一种能够消除盲反演不确定性、实现稳健高效像差测量的技术方案。

## 发明内容

针对现有物镜的像差标定技术存在的收敛速度慢、盲反演不确定性和像差标定精度低等问题，本发明提供了一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法及系统，可以实现无歧义、稳健高效的像差标定。

一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法，其特征在于，包括以下步骤：

S1、利用空间光调制器对经过准直和偏振处理的相干光源进行相位调制，生成一系列确定性散斑图案作为确定性散斑照明场；控制空间光调制器对确定性散斑图案进行平移，并同步采集经待测物镜成像的低分辨率图像序列；

S2、对采集到的低分辨率图像序列进行预处理，初始化样品的复振幅估计值和待测物镜的相干传递函数；根据空间光调制器加载的原始散斑图案，利用傅里叶相移定理计算各平移位置对应的理论照明场复振幅分布；

S3、建立包含样品复振幅、确定性散斑照明场及物镜相干传递函数的联合优化模型，并利用确定性散斑照明场的复振幅分布作为空间域强约束；

S4、通过反向传播误差信号，同步更新样品的复振幅以及物镜相干传递函数的相位分布及振幅分布；执行多轮次迭代，直至目标损失函数收敛或达到预设的最大迭代次数；

S5、提取步骤S4恢复得到的复值相干传递函数的相位部分，拟合成泽尼克多项式以量化分解待测物镜的像差，并结合恢复出的样品振幅和相位信息进行系统性能评估。

优选地，步骤S1中，所述的确定性散斑图案是通过计算机预先生成并加载至空间光调制器的灰度图或相位图，其生成算法采用菲涅尔衍射传播、Gerchberg-Saxton算法或伪随机相位矩阵经傅里叶变换；确保图案的统计特性（如散斑颗粒大小、对比度）可控。相较于传统散射片产生的随机且不可复现的散斑，本发明中的散斑图案具有数学上的确定性与可重复性，消除了照明场未知带来的“盲反演”病态问题。

进行平移的策略包括沿X轴和Y轴的亚像素级步进平移，平移步长设置为物镜截止频率对应的奈奎斯特采样间隔的分数倍，以确保频域覆盖的交叠率满足重建要求，同时有效抑制频谱混叠伪影。

进一步优选地，为了适应不同数值孔径（NA）的待测物镜，所述确定性散斑图案的空间频率分量被设计为覆盖从直流分量至超过物镜衍射极限的高频区域。通过精确控制散斑颗粒的平均尺寸，可以定制照明光的角谱范围。当照明光平移时，样品被这些精细结构照射，其携带的高频信息被编码并混叠至物镜的通带内，从而在后续频域拼接过程中，能够合成一个远超越物镜原始衍射极限的宽频谱，实现超分辨重建。

步骤S2中，所述预处理包括暗场校正、平场校正及坏点像素剔除；

初始化样品的复振幅估计值和待测物镜的相干传递函数具体为：基于所有采集图像的强度平方根平均值初始化样品的复振幅估计值（即振幅取平均值，相位初始化为0）；将待测物镜的相干传递函数初始化为理想圆瞳函数（通带内振幅为1，相位为0）或基于设计参数的先验估计值。

根据空间光调制器（SLM）加载的原始散斑图案，利用傅里叶相移定理计算各平移位置对应的理论照明场复振幅分布，为后续迭代提供精确的照明约束。

步骤S3中，联合优化模型采用改进的交替投影算法进行迭代更新，具体包括：

在空间域中，将当前估计的样品复振幅与已知平移后的确定性散斑照明场复振幅相乘，计算出射波前；这一步利用了照明场的精确已知性，对波前进行了严格的物理约束；

在傅里叶域中，将出射波前变换至频域，并与当前估计的物镜相干传递函数相乘，模拟光瞳低通滤波效应，随后用实际采集的低分辨率图像强度的平方根替换滤波后波前的振幅，同时保留其相位，从而引入傅里叶域的模量约束。

优选地，步骤S4中，为了解决迭代过程中的噪声放大与不收敛问题，所述同步更新引入自适应权重更新机制，在更新物镜相干传递函数时，根据频谱信噪比动态调整更新步长：在低频区域，由于信号能量集中、信噪比高，采用较大的更新步长；在高频区域，信号弱、噪声占比高，采用较小的更新步长；并结合总变分正则化或稀疏约束以防止噪声放大，确保恢复出的CTF具有物理合理性和高保真度。

步骤S4中，所述目标损失函数定义为重建强度图像与实测强度图像之间的均方误差或泊松似然函数；收敛条件为连续两次迭代间的损失函数变化量小于预设阈值，或者恢复出的波像差RMS值在连续多次迭代中变化小于0.001λ，以确保结果的稳定性。

步骤S5中，所述量化分解待测物镜的像差具体包括分解出离焦、像散、彗差及球差项；

所述系统性能评估包括对标定后的成像系统进行调制传递函数分析和信噪比评估。

一种基于确定性散斑照明的傅里叶叠层物镜像差标定系统，用于执行上述傅里叶叠层物镜像差标定方法，其特征在于，包括：

激光准直偏振模块，用于产生高相干性、偏振态稳定的平行光束；

空间光调制模块，与所述激光准直偏振模块光路连接，用于生成并动态调控确定性散斑照明场；所述空间光调制模块包括空间光调制器、偏振控制单元及折叠反射镜；

成像模块，用于接收经样品调制的光束并记录低分辨率强度图像；所述成像模块包括待测物镜、管状透镜及图像传感器；

迭代恢复算法模块，部署于计算单元中，内置有散斑图案生成算法及联合优化重建算法；所述迭代恢复算法模块用于控制空间光调制器和图像传感器同步工作，并利用已知的确定性散斑照明作为先验约束，执行迭代恢复流程，输出样品高分辨率复振幅图像及待测物镜的复值相干传递函数。

进一步地，所述激光准直偏振模块包括单纵模连续波激光光源、由第一透镜和第二透镜构成的扩束准直透镜组，以及起偏器；激光器发出的高斯光束首先经透镜组扩束并准直为大口径平面波，再通过起偏器形成振动方向特定的线偏振光；

若使用宽光谱光源，所述激光准直偏振模块还包括插入光路中的窄带滤光片，以确保照明光具有良好的时间相干性，满足相位恢复的要求。

为了实现高精度测量，所述激光光源为单纵模连续波激光器，其波长稳定性优于0.1nm，相干长度大于系统光程差，以满足高精度相位恢复对严格时间相干性的要求。

进一步地，所述空间光调制模块中的偏振控制单元包括二分之一波片，用于旋转入射光的偏振方向以匹配空间光调制器的液晶分子长轴方向；

所述空间光调制器为反射式相位型LCOS-SLM，其刷新率不低于60Hz，填充因子大于95%，以实现快速的照明图案切换和高光能利用率。

其工作原理为：从起偏器出射的线偏振光，首先通过二分之一波片（HWP）精确旋转偏振方向，使其与SLM的液晶分子长轴方向对齐，以确保最高效的相位调制效率。调整好偏振态的光束经反射镜反射，以与SLM法线呈小角度入射至SLM表面。SLM上加载的散斑图案对入射光波前进行相位调制。被调制的反射光携带了散斑信息，作为结构化照明场照射至样品平面。本模块实现了照明场的全数字化、高精度控制。

进一步地，所述图像传感器为科学级CMOS相机或CCD相机，具备高位深（≥12 bit）、低读出噪声（\<2 e-）和高量子效率（\>70%），以精确捕捉微弱的强度变化细节，尤其是在照明光平移至边缘时信号强度下降的情况。

待测物镜与管状透镜构成标准的4f傅里叶成像系统，其中物镜的后焦面为傅里叶频谱面，相机芯片位于系统的共轭像面。经样品调制的出射光，其频谱被物镜光瞳（即待测的CTF）自然低通滤波，最终在相机上形成低分辨率的强度图像。

进一步地，所述迭代恢复算法模块还配置为将恢复的复值相干传递函数的相位部分拟合成泽尼克多项式系数，并以三维波前图或彩色编码图的形式展示像差分布。

迭代恢复算法模块通过同步控制信号线与图像传感器连接，实时接收采集到的低分辨率图像序列。利用已知的确定性散斑照明作为先验约束，执行迭代恢复流程，端到端地输出两个核心结果：一是突破物镜衍射极限的样品高分辨率复振幅图像；二是包含待测物镜精确波像差的复值相干传递函数。

与现有技术相比，本发明具有以下有益效果：

1、消除盲反演不确定性，收敛速度快：本发明摒弃了传统随机散斑照明中照明场未知的“全盲”设定，通过SLM生成精确已知的确定性散斑，将复杂的三变量盲反演问题（样品、照明、光瞳）转化为约束性更强的半盲恢复问题。实验表明，该方法在相同迭代次数下，收敛速度提升显著，且不易陷入局部最优解。

2、高精度像差标定，抗噪性能强：利用确定性散斑的高频编码特性及空间域强约束，算法能够有效分离样品结构与系统像差。即使在低信噪比环境下，仍能准确恢复出物镜的细微波前畸变，标定精度可达$\lambda/50$RMS以上，优于传统LED阵列FPM方案。

3、系统结构灵活，无需复杂硬件标定：不同于LED阵列FPM需要精确标定每个LED的物理位置和发光角度，本发明仅需通过软件加载预设图案即可改变照明状态，避免了繁琐的硬件几何标定过程。同时，基于SLM的数字化控制使得照明模式（如平移步长、散斑粒度）可灵活调整，适应不同NA物镜的标定需求。

4、非干涉式测量，环境适应性好：本方法基于强度测量而非干涉条纹分析，对机械振动和环境扰动不敏感，无需隔振光学平台即可在常规实验室环境下实现高精度的物镜像差检测，具有极高的工程应用价值。

## 附图说明

为了更清楚地说明本发明实施例中的技术方案，下面将对实施例描述中所需要使用的附图作简单地介绍，显而易见地，下面描述中的附图仅仅是本发明的一些实施例，对于本领域普通技术人员来讲，在不付出创造性劳动的前提下，还可以根据这些附图获得其他的附图。

图1为本发明实施例一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法流程图。

图2为本发明实施例中迭代重建算法逻辑流程图。

图3为本发明实施例中平移后的散斑图案和对应拍摄的低分辨率图样例。

图4为本发明实施例一种基于确定性散斑照明的傅里叶叠层物镜像差标定系统的结构和原理图。

## 具体实施方式

下面将结合本发明实施例中的附图，对本发明实施例中的技术方案进行清楚、完整地描述，显然，所描述的实施例仅仅是本发明一部分实施例，而不是全部的实施例。基于本发明中的实施例，本领域普通技术人员在没有做出创造性劳动前提下所获得的所有其他实施例，都属于本发明保护的范围。

需要说明的是，在不冲突的情况下，下述的实施例及实施方式中的特征可以相互组合。

本实施例提出了一种利用空间光调制器生成确定性散斑照明场，结合傅里叶叠层成像技术进行环形光瞳物镜像差标定的方法。该方法针对现有技术在收敛速度、盲反演不确定性及标定精度上的不足，通过引入已知的结构化照明约束，将传统的全盲反演问题转化为半盲恢复问题，实现了无歧义、稳健且高效的像差测量。

如图1所示，一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法，包括以下步骤：

S1、构建确定性散斑照明场并采集低分辨率图像序列。由于傅里叶叠层成像需要多样化的照明角度或模式来合成高频信息，而传统随机散斑存在不可复现的“盲”特性，本实施例首先利用空间光调制器生成精确可控的确定性散斑。

具体地，系统启动后，激光准直偏振模块产生高相干性线偏振光。光束进入空间光调制模块，经二分之一波片调整偏振态以匹配反射式空间光调制器的最佳调制效率。

第一步，散斑图案生成。计算机预先生成一系列确定性散斑图案（灰度图或相位图）。这些图案可通过菲涅尔衍射传播模拟、Gerchberg-Saxton (GS) 算法迭代优化，或直接由伪随机相位矩阵经傅里叶变换得到。关键在于确保生成的散斑具有可控的统计特性（如颗粒大小、对比度），且数学上完全确定。

第二步，照明场加载与平移。将生成的散斑图案加载至SLM。为了在频域覆盖足够的重叠率以满足重建条件，本实施例采用亚像素级平移策略。利用傅里叶相移定理，在频域对散斑谱施加线性相位因子$e^{- i2\pi\left( ux_{m} + vy_{m} \right)}$，从而在空间域实现精确的平移$(x_{m},y_{m})$，避免了物理移动光源带来的机械误差和插值噪声。平移步长设定为物镜截止频率对应奈奎斯特间隔的分数倍（如1/2或1/4像素）。

第三步，图像采集。经过SLM调制的确定性散斑光照射到样品上，透过样品的光束携带了样品的高频信息。该光束经由待测物镜收集，在傅里叶频谱面受到物镜光瞳函数（即待测CTF）的低通滤波，最终由成像模块中的科学级CCD相机记录下一系列低分辨率强度图像$I_{m}(x,y)$。

S2、数据预处理与初始化。为了消除传感器噪声并给迭代算法提供合理的初始猜测，需对原始数据进行严格处理。

首先，数据清洗。对采集到的低分辨率图像序列进行暗场校正（减去背景底噪）、平场校正（消除光照不均匀）及坏点像素剔除，确保输入数据的纯净度。

其次，样品复振幅初始化。计算所有采集图像强度平方根的平均值，作为样品振幅的初始估计；将样品相位初始化为0。即$\psi_{obj}^{(0)}(x,y) = \sqrt{\frac{1}{M}\sum I_{m}(x,y)} \cdot e^{i0}$。

最后，CTF初始化。将待测物镜的相干传递函数（CTF）初始化为理想圆瞳函数（通带内振幅为1，相位为0），或者根据物镜的设计参数（如数值孔径NA、设计波长$\lambda$）生成一个包含基础像差的先验估计值。同时，根据SLM加载的原始散斑图案和平移量，计算出每一帧对应的理论照明场复振幅分布$P_{m}(x,y)$，作为后续迭代的已知强约束。

S3、构建迭代相位恢复与像差求解模型。本步骤建立了一个联合优化模型，旨在同时求解样品的高分辨率复振幅和物镜的复值CTF。该模型的核心在于利用已知的确定性照明场 $P_{m}$ 打破传统FPM中的变量模糊性。模型在空间域和傅里叶域之间交替投影：

S4、模型迭代更新与收敛判断。通过多轮次迭代，同步更新样品信息和物镜像差，直至解收敛。

具体更新过程如下：

1、误差反向传播：计算更新后的波前与更新前波前之间的差异，将该差异信号反传至频域。

2、自适应权重更新：利用梯度下降或自适应动量算法，分别修正样品频谱和物镜CTF。

3、收敛判断：重复上述步骤，形成闭环迭代。定义损失函数为重建强度图像与实测强度图像之间的均方误差（MSE）或泊松似然函数。当连续两次迭代间的损失函数变化量小于预设阈值（如 $10^{- 6}$ ），或恢复出的波像差RMS值在连续多次迭代中变化小于 $0.001\lambda$ 时，判定收敛。

最终输出两部分结果：一是突破衍射极限的样品高分辨率复振幅图像；二是包含高精度波像差信息的物镜复值CTF。

S5、像差可视化与系统性能评估。对恢复得到的复值CTF进行解析，量化评估物镜性能。

首先，泽尼克多项式拟合。提取CTF的相位部分 $\phi\left( k_{x},k_{y} \right)$ ，将其拟合成泽尼克（Zernike）多项式。通过分解系数，可以定量计算出待测物镜的各种初级像差（如离焦 $Z_{4}$ 、像散 $Z_{5}/Z_{6}$ 、彗差 $Z_{7}/Z_{8}$ 、球差 $Z_{9}$ 等）和高阶像差。

其次，可视化展示。将像差分布以三维波前图或彩色编码图的形式直观展示给用户，帮助快速定位光学系统的缺陷。

最后，像差分析。结合恢复出的样品高分辨率细节信息，对标定后的成像系统进行调制传递函数（MTF）分析和信噪比评估。

下面对本发明提到的迭代重建算法进行详细介绍，主要包括以下模块：

初始化与数据获取模块，用于输入投影在空间光调制器上的确定性散斑图案序列，以及同步采集的、对应于各散斑照明场下的样品低分辨率强度图像序列；基于所有捕获图像的强度分布进行平均，初始化样品复振幅的振幅信息；

散斑平移调制模块，用于根据傅里叶相移定理，在频域中对已知的散斑图案施加线性相位因子，生成平移后的散斑照明场，实现空间域中散斑图案的精确平移，避免反复在空间域与频域之间进行图像插值操作引入的数值误差；

空间域前向传播模块，用于将当前估计的样品出射波前与已知的平移散斑照明场相乘，得到调制后的复振幅分布；将该复振幅分布通过自由空间传播至物镜后焦面，经待测物镜的相干传递函数进行低通滤波，再通过逆傅里叶变换得到像面复振幅分布；

频域模量约束模块，用于以实际采集的低分辨率强度图像的平方根替代所述像面复振幅分布的振幅，保留其相位信息，形成更新后的像面复振幅分布；利用该更新后的复振幅分布反向传播至频域，分别更新样品频谱和待测物镜的相干传递函数；

更新与迭代控制模块，用于在频域中基于预设的更新权重参数对样品频谱和相干传递函数进行加权更新，控制迭代步长与收敛稳定性；将更新后的频谱逆变换回空间域，得到更新后的样品出射波前，并作为下一次迭代的初始值。

重复上述各模块的迭代过程，直至相干传递函数的变化量低于预设阈值或达到预设迭代次数，输出最终恢复的待测物镜相干传递函数，进而得到物镜的光瞳函数及波像差。

该迭代重建算法通过引入已知的确定性散斑照明场作为强约束，将传统傅里叶叠层中的盲反演问题转化为半盲恢复问题，具有收敛速度快、重建精度高、抗噪性能强的特点。

本发明实施例中，在初始化数据获取模块，设散斑图案的函数为$\varphi(x,y)$，即对应中输入的散斑图案。

在散斑平移调制模块，要在空间域中实现移动散斑图案，需要将散斑图案分别沿着x轴和y轴平移a和b个单位，有下列变化过程：

$\Psi(u,v) = \mathcal{F}\left\lbrack \varphi(x,y) \right\rbrack$；

在频率域中的频率改变量为：

$P(u,v) = exp\left\lbrack - 2i\pi \cdot (ua + vb) \right\rbrack$；

对频率域中的散斑施加该变量：

$\Psi^{'}(u,v) = \Psi(u,v) \cdot P(u,v)$；

再变换到空间域中，即得到平移后的散斑图案：

$\varphi^{'}(x,y) = \mathcal{F}^{- 1}\left( \Psi^{'}(u,v) \right)$；

在空间域前向传播模块，设在SLM生成的散斑图案矩阵为$S(x,y)$，即对应图2中的Speckle$(x,y)$经过该散斑图调制的相干光再穿过待测物镜到达CCD相机形成强度图像。可以将形成的强度图像矩阵表达为：

$I_{n}(x,y) = \left| \left\lbrack \left( S\left( x - x_{n},y - y_{n} \right) \otimes P_{\text{PSF}}(d) \right) \cdot O(x,y) \right\rbrack \otimes P_{\text{PSF}}(x,y) \right|^{2}$；

其中，$\otimes$表示卷积，“.”表示元素间的乘积，$I_{n}(x,y)$是由CCD相机获得的第n张图片的强度矩阵，$O(x,y)$表示为样品复振幅函数，$P_{\text{PSF}}(d)$代表自由传播d距离后的点扩散函数（PSF），d是SLM到样品的距离，$（x_{n}，y_{n}）$是SLM生成的散斑图案的第n次位移后的位置，$P_{\text{PSF}}(x,y)$表示待测物镜的点扩散函数，经过傅里叶变换后为物镜的CTF。

在频域模量约束模块，基于生成的散斑图案移动到不同位置的所有捕获图像，如图3所示，复出射波前可以通过阶段检索过程来恢复。图3中，其中(a1)、(a2)、(a3)是移动的散斑图案，(b1)、(b2)、(b3)是对应拍摄的低分辨率图像。在相位检索过程开始之前，物体的复振幅函数由对所有捕获的强度分布进行平均。第m次迭代更新后的复振幅函数为$O_{m}(x,y)$，定义$S^{'}\left( x - x_{n},y - y_{n} \right)$为：

$S^{'}\left( x - x_{n},y - y_{n} \right) = S\left( x - x_{n},y - y_{n} \right) \otimes P_{\text{PSF}}(d)$；

被SLM调制后的出射波前可以被表示为:

$\phi_{m}(x,y) = O_{m}(x,y) \cdot S^{'}\left( x - x_{n},y - y_{n} \right)$；

$\Phi_{m}\left( k_{x},k_{y} \right) = \mathcal{F}\left\{ \phi_{m}(x,y) \right\}$；

其中$\mathcal{F}$代表傅里叶变换，频谱$\Phi_{m}$被CTF低通滤波后得到的复出射波前$\Psi_{m}$:

$\Psi_{m}\left( k_{x},k_{y} \right) = \Phi_{m}\left( k_{x},k_{y} \right) \cdot C_{\text{CTF}}\left( k_{x},k_{y} \right)$；

$\psi_{m}(x,y) = \mathcal{F}^{- 1}\left\{ \Psi_{m}\left( k_{x},k_{y} \right) \right\}$；

其中$\mathcal{F}^{- 1}$代表傅里叶逆变换。随后，用拍摄到的图像强度$I_{m}$的根号代替$\psi_{m}$的振幅，但保持其相位不变：

$\psi_{m}^{'}(x,y) = \psi_{m}(x,y)/\left| \psi_{m}(x,y) \right| \cdot \sqrt{I_{m}}$；

$\Psi_{m}^{'}\left( k_{x},k_{y} \right) = \mathcal{F}\left\{ \psi_{m}^{'}(x,y) \right\}$；

在更新与迭代控制模块，运用以下公式在傅立叶域中更新频谱$\Phi_{m}$和CTF（对应图2中的$Pupil_{m}$）:

$\Phi_{m}^{'}\left( k_{x},k_{y} \right) = \Phi_{m}\left( k_{x},k_{y} \right) + \alpha\left( \Psi_{m}^{'}\left( k_{x},k_{y} \right) - \Psi_{m}\left( k_{x},k_{y} \right) \right) \cdot \left( C_{CTF}\left( k_{x},k_{y} \right) \right)^{- 1}/\left( \left| C_{CTF}\left( k_{x},k_{y} \right) \right|^{2} \right)_{\max}$；${C^{’}}_{\text{CTF}}\left( k_{x},k_{y} \right) = C_{\text{CTF}}\left( k_{x},k_{y} \right) + \beta\left( \Psi_{m}^{'}\left( k_{x},k_{y} \right) - \Psi_{m}\left( k_{x},k_{y} \right) \right) \cdot \left( \Phi_{m}^{'}\left( k_{x},k_{y} \right) \right)^{- 1}/\left( \left| \Phi_{m}^{'}\left( k_{x},k_{y} \right) \right|^{2} \right)_{\max}$；

其中，$\alpha$、$\beta$为更新权重参数，它们在迭代更新中起到控制步长与收敛稳定性的作用。之后更新的出射波前$\phi_{m}^{'}$可以从傅里叶变换得到:

$\phi_{m}^{'}(x,y) = \mathcal{F}^{- 1}\left\{ \Phi_{m}^{'}\left( k_{x},k_{y} \right) \right\}$；

再多次迭代之后即可恢复待测物镜的CTF，即得到其光瞳函数。再更新:

$O_{m + 1}(x,y) = O_{m}(x,y) + \left( \phi_{m}^{'}(x,y) - \phi_{m}(x,y) \right) \cdot S^{'}\left( x - x_{n},y - y_{n} \right)^{- 1}/\left( (1 - \gamma) \cdot \left| S^{'}\left( x - x_{n},y - y_{n} \right) \right|^{2} + \gamma \cdot \left| S^{'}\left( x - x_{n},y - y_{n} \right) \right|_{\max}^{2} \right)$；

其中，$\gamma$为更新权重系数。即可进行下一轮物镜CTF的迭代计算，依次迭代直至收敛。

本发明实施例还提供一种基于确定性散斑照明的傅里叶叠层物镜像差标定系统。该系统旨在通过高精度的光学组件与先进的计算算法相结合，实现对环形光瞳物镜像差的非干涉式、高灵敏度测量。

如图4所示，本实施例提供的系统主要包含四个核心部分：激光准直偏振模块100、空间光调制模块200、成像模块300以及迭代恢复算法模块400。各模块在物理空间上依次排列，并通过光路与数据线缆连接，形成一个闭环的测量与处理系统。

1、激光准直偏振模块100：该模块负责产生高质量、偏振态稳定的相干照明光源，是保证后续相位调制效率的基础。

a）激光光源101：选用单纵模连续波激光器（例如波长为532nm或632.8nm），其波长稳定性优于0.1nm，相干长度大于系统最大光程差，以满足高精度相位恢复对严格时间相干性的要求。若使用宽光谱光源，需在此处增加窄带滤光片。

b）扩束透镜组：由第一透镜102（L<sub>1</sub>）和第二透镜103（L<sub>2</sub>）组成倒置望远镜结构。激光束首先经第一透镜102聚焦，再经第二透镜103准直，将高斯光束整型为光斑直径较大（如覆盖SLM有效孔径）且波前平整的高质量平面波。

c）起偏器104：位于扩束光路之后，用于将自然光或非理想偏振光过滤为振动方向单一的线偏振光，为进入空间光调制模块做准备。

2、空间光调制模块200：该模块是系统的核心照明控制单元，负责生成并动态调控确定性散斑照明场。

a）二分之一波片201（HWP）：位于起偏器之后。由于反射式相位型SLM通常仅对特定方向（如水平方向）的线偏振光有相位调制作用，而对正交方向仅表现为普通反射镜。因此，通过旋转HWP，可以精密调节入射光的偏振方向，使其与SLM液晶分子的长轴方向精确匹配，从而实现最高的相位调制效率。

b）折叠反射镜202（M2）：用于改变光路传播方向，使光束以特定角度入射至SLM表面。在本实施例中，光束经M2反射后，以与SLM法线成约7°的角度入射（具体角度视SLM型号而定），以便于分离入射光与调制后的反射光。

c）空间光调制器203（SLM）：选用高分辨率、高填充因子（\>95%）的反射式相位型LCOS-SLM。SLM加载由计算机预先生成的确定性散斑图案（灰度图或相位图）。入射光在SLM表面反射时，其波前被加载的图案精确调制，转化为携带特定高频信息的确定性散斑场。SLM的刷新率不低于60Hz，支持快速的图案切换以实现亚像素级平移扫描。

3、成像模块300：该模块负责接收经样品调制的光束，并将其转换为数字图像信号。

a）待测物镜301：位于样品后方，是本次标定的目标对象。它收集透过样品的散射光，并在其后焦面（傅里叶频谱面）处对光场进行低通滤波。物镜的数值孔径（NA）决定了系统的理论分辨率极限。

b）管状透镜302：与待测物镜共同构成标准的4f傅里叶成像系统。它将物镜后焦面的频谱信息传递至像面，确保CCD位于准确的共轭像平面上。

c）图像传感器303（CCD）：选用科学级CMOS或CCD相机，具备高位深（≥12 bit）、低读出噪声（\<2 e-）和高量子效率（\>70%）。相机用于同步采集在不同散斑照明位置下的一系列低分辨率强度图像。这些图像不仅包含样品的结构信息，还编码了物镜的像差信息。

4、迭代恢复算法模块400：该模块部署于高性能工作站计算机中，是系统的“大脑”，负责控制硬件运行并执行核心重建算法。

a）硬件信息采集部分401：包括计算机主机、GPU加速卡以及相机和SLM控制接口。与此同时，也负责采集低分辨率图像，并进行裁剪和归一化等预处理操作。

b）控制软件部分402：负责协调SLM图案的加载、平移量的计算以及CCD图像的触发采集，确保照明与采样的严格同步。

c）迭代恢复算法部分403：内置本发明核心的联合优化重建算法（流程如图2所示）。

本实施例系统通过引入确定性散斑照明，成功将传统傅里叶叠层中的“全盲”反演问题转化为“半盲”恢复问题。相较于传统LED阵列方案，本系统无需复杂的几何标定，光路结构紧凑灵活；相较于随机散斑方案，本系统具有收敛速度快、解的唯一性强、抗噪性能优越等显著特点，特别适用于高精度显微物镜的在线检测与标定。

以上所述的实施例对本发明的技术方案和有益效果进行了详细说明，应理解的是以上所述仅为本发明的具体实施例，并不用于限制本发明，凡在本发明的原则范围内所做的任何修改、补充和等同替换，均应包含在本发明的保护范围之内。

# 说明书摘要

本发明公开了一种基于确定性散斑照明的傅里叶叠层物镜像差标定方法及系统，该方法通过空间光调制器SLM对相干光源进行调制，生成一系列精确已知且可重复的确定性散斑图案作为确定性散斑照明场；控制SLM对散斑图案进行精确平移，并同步采集经待测物镜成像的一系列低分辨率强度图像；基于已知的散斑照明场作为空间域强约束，利用迭代相位恢复算法，在重建样品高分辨率复振幅信息的同时，迭代求解出待测物镜的相干传递函数CTF，进而获得物镜的光瞳函数及波像差。本发明将传统傅里叶叠层中的盲反演问题转化为约束性更强的半盲恢复问题，无需复杂的LED阵列标定，具有收敛速度快、恢复精度高、抗噪性能强等优点，适用于高精度显微物镜的非干涉式标定。

# 说明书附图

![图1](media/image1.png)

图1

![图2](media/image3.png)

图2

![图3](media/image5.jpeg)

图3

![图4](media/image6.png)

图4

指定图1为摘要附图
